name: Build and Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release tag (e.g. v0.1.0)'
        required: true

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            platform: linux
            artifact_name: linux
            bundles: deb
            paths: |
              src-tauri/target/release/bundle/deb/*.deb
          - os: windows-latest
            platform: windows
            artifact_name: windows
            bundles: nsis
            paths: |
              src-tauri/target/release/bundle/nsis/*.exe
          - os: macos-14
            platform: macos
            artifact_name: macos-arm64
            bundles: dmg
            target: aarch64-apple-darwin
            paths: |
              src-tauri/target/aarch64-apple-darwin/release/bundle/dmg/*.dmg
          - os: macos-14
            platform: macos
            artifact_name: macos-x64
            bundles: dmg
            target: x86_64-apple-darwin
            paths: |
              src-tauri/target/x86_64-apple-darwin/release/bundle/dmg/*.dmg

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install macOS Rust target
        if: matrix.platform == 'macos'
        run: rustup target add ${{ matrix.target }}

      - name: Install Linux dependencies
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update && sudo apt-get upgrade -y
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Install dependencies
        run: npm ci

      - name: Build Tauri app
        run: |
          ARGS="--bundles ${{ matrix.bundles }}"
          if [ -n "${{ matrix.target }}" ]; then
            ARGS="$ARGS --target ${{ matrix.target }}"
          fi
          npm run tauri build -- $ARGS
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: rigtree-fetch-${{ matrix.artifact_name }}
          path: ${{ matrix.paths }}

  arch:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set PKGBUILD version
        run: |
          VERSION=$(echo "${{ github.event.inputs.version }}" | sed 's/^v//')
          sed -i "s/^pkgver=.*/pkgver=$VERSION/" PKGBUILD

      - name: Build Arch package in container
        run: |
          docker run --rm \
            -v "$PWD:/src" -w /src \
            archlinux:latest \
            bash -c '
              pacman -Syu --noconfirm --needed \
                base-devel nodejs npm rust \
                webkit2gtk libappindicator-gtk3 libayatana-appindicator libxdo openssl librsvg
              useradd -m builder
              chown -R builder:builder /src
              su builder -c "cd /src && makepkg -sf --noconfirm"
            '

      - name: Upload Arch artifact
        uses: actions/upload-artifact@v4
        with:
          name: rigtree-fetch-arch
          path: "*.pkg.tar.zst"

  release:
    needs: [build, arch]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: find artifacts -type f

      - name: Rename artifacts to RigTree-Fetch_$VERSION
        run: |
          VERSION="${{ github.event.inputs.version }}"
          mkdir -p release
          for dir in artifacts/*/; do
            name=$(basename "$dir")
            for f in "$dir"*; do
              [ -f "$f" ] || continue
              base=$(basename "$f")
              ext="${base##*.}"
              case "$name" in
                rigtree-fetch-linux)   suffix="_amd64.$ext" ;;
                rigtree-fetch-windows) suffix=".$ext" ;;
                rigtree-fetch-macos-arm64) suffix="_arm64.$ext" ;;
                rigtree-fetch-macos-x64)  suffix="_x64.$ext" ;;
                rigtree-fetch-arch)    suffix="_x86_64.pkg.tar.zst" ;;
                *) suffix=".$ext" ;;
              esac
              cp "$f" "release/RigTree-Fetch_${VERSION}${suffix}"
            done
          done
          find release -type f

      - name: Generate release body from commits
        id: changelog
        run: |
          PREV=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          if [ -z "$PREV" ]; then
            BODY=$(git log --pretty=format:"- %s")
          else
            BODY=$(git log "$PREV"..HEAD --pretty=format:"- %s")
          fi
          echo "$BODY" > release-body.md

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.version }}
          name: Release ${{ github.event.inputs.version }}
          body_path: release-body.md
          files: release/*
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
